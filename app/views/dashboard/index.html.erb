<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link rel="stylesheet" type="text/css" href="dashboard.css">
    
  </head>
  <body>
    <nav class="navbar navbar-default navbar-fixed-top " id="dashboard-nav">
      <div class="container-fluid">
        <div class="navbar-header">
          <img src="PC.png" class="navbar-brand"/> 
        </div>
        <ul class="nav navbar-nav">
          <li><a href="#">PharmaC</a></li>
        </ul>
        
        <form class="navbar-form navbar-left" id="dashboard-search-bar" method="get">
          <div class="input-group">
            <input type="text" name="search" class="form-control" placeholder=<%= params[:search]%>>
            <div class="input-group-btn">
              <button class="btn btn-default" type="submit">
                <i class="glyphicon glyphicon-search"></i>
              </button>
            </div>
          </div>
        </form>
        <a href="/create_med">Create a medicine</a>
        <ul class="nav navbar-nav navbar-right" id="dashboard-user">
          <li><a href="#" id="user-data"></a>
          </li>
        </ul>
      </div>
    </nav>
    <div class="container body">
      <div class="function-buttons">
        <form class="navbar-form navbar-left" method="get">
          <div class="input-group-btn">
            <button name="sort" value="brand" class="btn btn-primary" type="submit">Sort By Brand</button>
            <button name="sort" value="quantity" class="btn btn-success" type="submit">Sort By Stocks</button>
            <button name="sort" value="price" class="btn btn-warning" type="submit">Sort By Price</button>
          </div>
        </form>
      </div>
      <table class="table table-striped" style="text-align: left;">
        <thead>
          <tr>
            <td>Generic Name</td>
            <td>Brand Name</td>
            <td>Stocks</td>
            <td>Dosage(mg/ml)</td>
            <td>Price</td>
            <td>Last Updated</td>
          </tr>
        </thead>
        <tbody>
          <% @medicine.each do |p| %>
          
            <tr>
              <td><%= p.generic %></td>
              <td><%= p.brand %></td>
              <td><%= p.quantity %></td>
              <td><%= p.mg %></td>
              <td>₱<%= p.price %></td>
              <td><%= p.updated_at.strftime("%B %d,%Y") %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  
  </body>
</html>